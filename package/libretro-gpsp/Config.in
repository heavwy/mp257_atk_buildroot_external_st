config BR2_PACKAGE_LIBRETRO_GPSP
    bool "libretro-gpsp"
    depends on BR2_INSTALL_LIBSTDCPP
    help
      A libretro GBA emulator core for ARM (Optimized with Dynarec).

      http://www.libretro.com

comment "libretro-gpsp needs a toolchain w/ C++"
    depends on !BR2_INSTALL_LIBSTDCPP